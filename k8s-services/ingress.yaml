apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kraken-api-ingress
spec:
  rules:
    - host: localhost
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: kraken-lb
                port:
                  number: 8090